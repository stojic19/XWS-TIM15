syntax = "proto3";

option csharp_namespace = "Messages_microservice.Protos";

package messages;

service Messages{
	rpc Get(GetRequest)returns(GetResponse);
	rpc Add(NewMessage)returns(Message);
}
message GetRequest{

}

message GetResponse{
	repeated Message message = 1;
}

message NewMessage{
	string text = 1;
}

message Message{
	string id = 1;
	string text = 2;
}
